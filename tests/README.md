# Tests

This folder is wired for future TestEZ or similar Luau-based unit tests. For now it only contains a placeholder. Add suites under `tests/` and run them from within the dev container once a test runner is chosen.

The current storyboard contract runs from `scripts/storyboard_contract.sh` so `scripts/check.sh` works in the shared Roblox toolchain container without adding Python.
